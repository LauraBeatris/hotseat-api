/**
 * Defines the time in minutes in order to expires
 * the JWT token
 */
export const JWT_TOKEN_EXPIRES_IN = '20m';

/**
 * Defines the time in minutes in order to expires
 * the JWT refresh token
 */
export const JWT_REFRESH_TOKEN_EXPIRES_IN = '7d';
